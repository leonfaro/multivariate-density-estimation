# Lade benötigte Pakete
library(benchden)     # Benchmark-Verteilungen
library(tram)         # Transformation Models
library(hdrcde)       # HDR-Dichteschätzung
library(np)           # Nonparametric Methods
library(ks)           # Kernel Smoothing
library(kdensity)     # Kernel Density Estimation
library(kedd)         # Kernel Estimation
library(lpdensity)    # Lokale polynomiale Dichte
library(torch)        # Für Normalizing Flows
library(torch)        # Für Normalizing Flows
library(rgl)          # 3D Visualisierung
install.packages("extRemes")
```{r setup, include=FALSE}
```{r setup, include=FALSE}
install.packages("extremefit")
install.packages("extRemes")
